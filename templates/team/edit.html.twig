{% extends 'base.html.twig' %}

{% block title %}Modifier equipe | CoachManager{% endblock %}

{% block body %}
    <div class="d-flex justify-content-between align-items-end mb-4">
        <div>
            <h1 class="cm-page-title">Modifier l equipe</h1>
            <p class="cm-page-subtitle">{{ team.nom }} - {{ team.type }}</p>
        </div>
    </div>
    <div class="cm-card p-4">
        {{ include('team/_form.html.twig', {'button_label': 'Mettre a jour'}) }}
        <div class="mt-3 pt-3 border-top border-secondary-subtle">
            {{ include('team/_delete_form.html.twig') }}
        </div>
    </div>
{% endblock %}
